import { PriorityClassPipe } from './priority-class.pipe';

describe('PriorityClassPipe', () => {
  it('create an instance', () => {
    const pipe = new PriorityClassPipe();
    expect(pipe).toBeTruthy();
  });
});
